For this app to run, you'll need a file `static/names.txt` containing a space-separated list of names.

# Running

```sh
./run.sh
```

You'll need a file `data.json` at the root of your project. TODO: describe format or make generating it easier

# Linting

```sh
# lint CSS
npx stylelint "**/*.css"
# lint JavaScript
npx eslint static/index.js
```
